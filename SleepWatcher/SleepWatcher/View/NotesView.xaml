<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Converters="clr-namespace:SleepWatcher.Converters"
             x:Class="SleepWatcher.View.NotesView"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="700"
             Template="{DynamicResource NotesTemplate}">
    <UserControl.Resources>
        <Converters:EnumStringToFriendlyNameConverter x:Key="EnumStringToFriendlyNameConverter" />
        <ControlTemplate x:Key="NotesTemplate">
            <Grid d:DataContext="{Binding Source={StaticResource NotesViewModelSample}}"
                  DataContext="{Binding NotesViewModel, Source={StaticResource Locator}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0"
                           Margin="5,0,5,5"
                           Style="{DynamicResource TitleTextBlockStyle}"
                           Text="Notes" />
                <Button Grid.Row="0"
                        Style="{DynamicResource BackButton}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="5,5,5,0"
                        Command="{Binding SinglePatientViewModel.SwitchToSinglePatientViewCommand, Mode=OneWay, Source={StaticResource Locator}}"
                        Height="25">
                    <Image Height="15"
                           Width="15"
                           Margin="5"
                           Source="../Resources/back.png" />
                </Button>

                <TextBlock Grid.Row="1"
                           HorizontalAlignment="Left"
                          
                           Style="{StaticResource TextBlockStyle}"
                           Foreground="#FF636363"
                           FontSize="32"
                           Margin="5,0,5,5"
                           Text="{Binding Patient.FullName}" VerticalAlignment="Center" />
                <TextBlock Grid.Row="1"
                           HorizontalAlignment="Right"
                           Margin="5,0,5,5"
                           Style="{StaticResource TextBlockStyle}" VerticalAlignment="Bottom"><Run Text="Step: "
                                                                        FontWeight="Bold" /><Run  Text="{Binding Step.StepName, Converter={StaticResource EnumStringToFriendlyNameConverter}}" /></TextBlock>
                <ListBox Grid.Row="2"
                         ItemsSource="{Binding Notes}"
                         ItemTemplate="{StaticResource NoteTemplate}"
                         SelectedItem="{Binding SingleNoteViewModel.Note, Mode=OneWayToSource, Source={StaticResource Locator}}" />
            </Grid>
        </ControlTemplate>
        <Style x:Key="TitleTextBlockStyle"
               TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground"
                    Value="#FFFF8001" />
            <Setter Property="FontFamily"
                    Value="Cambria" />
            <Setter Property="FontSize"
                    Value="29.333" />
            <Setter Property="Margin"
                    Value="5,5,0,0" />
        </Style>
    </UserControl.Resources>
</UserControl>
